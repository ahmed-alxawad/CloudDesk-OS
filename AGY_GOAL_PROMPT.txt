Finish CloudDesk-OS v1.0 from the exact point Codex stopped.

Read `AGY_HANDOFF.md` completely first. Then read the five specifications under `Architecture/CloudDesk-OS-spec/` in the order defined by the handoff and inspect the existing root `AGENTS.md`.

Inspect Git status/history/diff and preserve all existing Codex work. Use the existing `graphify-out/` as supplemental architecture context and update it using the installed Graphify CLI if available.

Codex's expected unfinished task is the Vault migration to true per-record envelope encryption. Verify that in the actual checkout, finish it with secure migration/version handling and security tests, and run strict Rust validation.

After the Vault is complete, DO NOT STOP. Re-read `Architecture/CloudDesk-OS-spec/PLAN.md`, find the earliest genuinely incomplete v1.0 release item, implement it, test it, then continue to the next. Keep doing this until the production v1.0 release gates genuinely pass or an unavoidable external blocker exists.

Do not redo completed work, discard uncommitted changes, weaken security, run `cloudeskd` as root, add a generic privileged shell API, store plaintext secrets, route remote transfers through the browser, or make heavyweight services mandatory.

If an unavoidable blocker or session/tool limit is reached, create `AGY_PROGRESS_CHECKPOINT.md` exactly as required by `AGY_HANDOFF.md` so another session can resume immediately.

