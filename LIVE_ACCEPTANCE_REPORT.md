# CloudDesk-OS Live Acceptance Report

Generated by executing the real product code (`clouddesk-remote`) against real disposable fixtures (`tests/acceptance/docker-compose.yml`). Every line below reflects an actual `Result` from a real call; nothing is hardcoded.

## Real MinIO/S3 (clouddesk_remote::s3::S3Provider)

- upload (new key): **PASS** (executed, succeeded)
- object listing: **PASS** (acceptance.txt present in ListObjectsV2 result)
- download / round-trip: **PASS** (bytes read back match bytes written)
- multipart upload >5 MB: **PASS** (executed, succeeded)
- multipart round-trip: **PASS** (6 MB payload read back byte-for-byte)
- delete: **PASS** (executed, succeeded)
- invalid credentials rejected: **PASS** (write with wrong credentials returned an error)

## Real WebDAV server (clouddesk_remote::webdav::WebDavProvider)

- PUT (new file): **PASS** (executed, succeeded)
- browse (PROPFIND): **PASS** (acceptance.txt present in listing)
- GET: **PASS** (bytes read back match bytes written)
- MKCOL: **FAIL** — `filesystem operation failed: HTTP Error: 405 Method Not Allowed`
- MOVE: **PASS** (executed, succeeded)
- DELETE: **PASS** (executed, succeeded)
- wrong credentials rejected: **PASS** (PUT with wrong password returned an error)

## Real OpenSSH server (clouddesk_remote::ssh::SshSession)

- password auth: **PASS** (executed, succeeded)
- wrong password rejected: **PASS** (authentication with an incorrect password returned an error)

## Real SFTP server (clouddesk_remote::sftp::SftpProvider — CLAUDE-NIGHTMARE-003/-004 regressions)

- list (root): **PASS** (executed, succeeded)
- upload (new file): **PASS** (executed, succeeded)
- download / round-trip: **PASS** (bytes match what was uploaded)
- mkdir: **FAIL** — `filesystem operation failed: Failure: Failure`
- rename: **PASS** (executed, succeeded)
- delete: **PASS** (executed, succeeded)
- large streamed file (4 MB) upload: **PASS** (executed, succeeded)
- large streamed file round-trip: **PASS** (4 MB payload matches byte-for-byte)

## SSH key-based authentication

- RSA private key auth: **PASS** (executed, succeeded)
- encrypted private key + passphrase auth: **PASS** (executed, succeeded)

## SSH host-key verification (CLAUDE-NIGHTMARE-002 regression)

- connection pinned to the real host key succeeds: **PASS** (executed, succeeded)
- connection pinned to a replaced/wrong host key is rejected: **PASS** (simulated host-key replacement / MITM was refused, not silently accepted)

## SSH authentication surface with no implementation

- SSH agent forwarding: **IMPLEMENTATION MISSING** — SshAuth::Agent immediately returns an error ("Agent auth not fully implemented via sockets yet") in crates/remote/src/ssh.rs — no socket-forwarding code exists
- keyboard-interactive authentication: **IMPLEMENTATION MISSING** — SshAuth::KeyboardInteractive immediately returns an error ("Keyboard interactive auth not implemented in russh 0.62") — no interactive prompt/response handling exists
- SSH certificate authentication: **IMPLEMENTATION MISSING** — SshAuth::Certificate decodes only key_data and silently ignores cert_data — no OpenSSH certificate parsing or validation exists; this is a facade, not an implementation
- ProxyJump / bastion hosts: **NOT EXECUTED** — SshSession::connect_proxyjump exists and is used by the new host-key-pinning regression tests, but has zero callers anywhere in services/clouddeskd — no HTTP endpoint or transfer path ever constructs a ProxyJump connection, so there is no product surface to exercise here

## Applications with no backend implementation

- FFmpeg probing / remux / transcoding: **IMPLEMENTATION MISSING** — no reference to ffmpeg exists anywhere in the Rust codebase (grep -r ffmpeg over every *.rs file is empty); stream_media/preview_media are plain HTTP byte-range file serving with no transcoding
- Video player application: **IMPLEMENTATION MISSING** — no Video app component exists in apps/web/src/lib; GalleryApp.svelte has no video-related code
- Music application: **IMPLEMENTATION MISSING** — no Music app, playlist, or ID3-metadata code exists anywhere in the repository (Rust or Svelte)
- Office editing runtime (LibreOffice/Collabora/WOPI): **IMPLEMENTATION MISSING** — crates/runtime is a JSON manifest schema/validator only; RuntimeDependency::Office is an enum variant with no launcher, container orchestration, or WOPI implementation behind it; no OfficeApp component exists
- VS Code-compatible runtime: **IMPLEMENTATION MISSING** — same manifest-only pattern as Office; RuntimeDependency::Code is an enum variant with no code-server process/container launcher; no CodeApp component exists
- Brave remote-browser runtime: **IMPLEMENTATION MISSING** — same manifest-only pattern; RuntimeDependency::Browser is an enum variant with no KasmVNC/Brave container launcher; no BrowserApp component exists
- Archive create/extract: **IMPLEMENTATION MISSING** — no zip/tar handling exists anywhere in the Rust codebase
- ACL viewing/editing: **IMPLEMENTATION MISSING** — ProviderFeature::Acl is declared as a capability flag but never backed by getfacl/setfacl or any ACL library call
- Resumable/chunked upload: **IMPLEMENTATION MISSING** — ProviderFeature::ResumableUpload is declared as a capability flag with no chunked-upload-session protocol behind it
- SCP transfers: **IMPLEMENTATION MISSING** — GOAL.md G9 requires SCP "where appropriate"; no scp-specific code exists (only SFTP)

## Conclusion

This report only ever reflects executed live checks against real disposable fixtures. It does not cover process-level UI flows, backup/restore, or the full v1.0 requirement set — see `RELEASE_EVIDENCE_AUDIT.md` and `V1_TRUE_CLOSURE.md` for the complete picture.
