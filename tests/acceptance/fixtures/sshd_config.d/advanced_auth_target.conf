# Task 13/35 (Phase 2 closure): trusts the same disposable test-only CA
# as the bastion, so the ProxyJump *target* hop can be authenticated
# with a certificate independently of the bastion hop's own (password)
# credential. See tests/acceptance/fixtures/sshd_config.d/advanced_auth.conf
# for the CA itself and the reasoning behind it -- this file only turns on
# TrustedUserCAKeys, not keyboard-interactive/PAM, since this fixture has
# no need for those.
TrustedUserCAKeys /config/ssh_ca/ca.pub
