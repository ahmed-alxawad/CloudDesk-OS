# Security tests

End-to-end authorization, path traversal, symlink, TOCTOU, secret-redaction, and
privileged-helper tests live here. Phase 0 unit-level invariant tests are colocated
with the crates that enforce them.

